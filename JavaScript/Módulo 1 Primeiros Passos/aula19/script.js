/*
let isMember = true;

let shipping = isMember ? 2 : 10;

console.log(isMember ? "Você é membro." : "Você não é membro.")
console.log("Frete: " + shipping);

if (isMember) {
    console.log("...")
}else { console.log("Seja um membro agora mesmo e economize no frete!")}
*/

let age = 17;

let isAdult = ((age >= 18) ? "Adulto" : "Menor de idade");

console.log(isAdult)